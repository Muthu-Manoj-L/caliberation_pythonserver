import WidgetModalDefault from './WidgetModal_NEW';

// Minimal re-export shim — keeps imports working while the original file
// is restored or replaced. This file must contain only valid TSX/TS code.
export const WidgetModal = WidgetModalDefault;
export default WidgetModalDefault;
